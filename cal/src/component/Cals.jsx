import React from 'react'
import Button from '@mui/material/Button';
import { createTheme } from '@mui/material/styles';
const Cals = () => {
    const theme = createTheme({
        
    })
  return (
    <div>
      <Button variant="contained" color="#3babae">
  Success
</Button>
    </div>
  )
}

export default Cals
